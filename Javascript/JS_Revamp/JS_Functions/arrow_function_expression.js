// A shorthand syntax introduced in ES6 for writing function expressions.
const sum = (a, b) => {
	return a + b;
};

console.log(sum(10, 20));

// one line arrow function
const add = (a, b) => a + b;
console.log(add(10, 20));
